{
  "name": "intventaris-barang-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend-app\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend-app",
    "install-all": "npm install --prefix backend && npm install --prefix frontend-app"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "concurrently": "^8.2.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.3.1"
  },
  "dependencies": {
    "axios": "^1.18.0",
    "lucide-react": "^1.20.0",
    "pdfkit": "^0.19.1",
    "react-router-dom": "^7.18.0"
  }
}
